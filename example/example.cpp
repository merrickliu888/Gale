// include necessary headers

int main() {
    // initialize http server
    // HttpServer server = new HttpServer();

    // Register routes
    // server.registerRoute(...);
    // server.registerRoute(...);

    // server.start();

    return 0;
}